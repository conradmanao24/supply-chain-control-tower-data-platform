# Simulation Scripts

This directory will contain project-owned wrappers or helpers for running the verified official WideWorldImporters simulation/workload used to create source changes for incremental-pipeline testing.

The project will not invent its own large random transaction generator as a replacement for the WWI source behavior.
