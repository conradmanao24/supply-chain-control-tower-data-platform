# Warehouse Bootstrap

PostgreSQL warehouse schemas and project-owned control objects are provisioned by the repository bootstrap and SQL migrations.

Runtime database storage is managed through Docker volumes and is intentionally excluded from Git.
